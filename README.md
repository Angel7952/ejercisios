simulacion y juegos 
en estas se utilizaron dos ligrerias que consiste:
#include <cstdlib>:contiene Contiene las funciones rand() y srand().
la funcion srand(): configura el inicio del generador de números aleatorios.
time(0): devuelve el tiempo actual → así cada ejecución da números distintos.
se utilizan para la la seccion de simulacion y juegos
generador de números aleatorios mediante distribución uniforme o normal en esta parte del codigo:
se utilizo una libreria include <random> que sirve para: generar números pseudoaleatorios de forma moderna y flexible
de resto todos fuen logicas con funciones, se utilizo las librerias:include <iostream>
using namespace std; que estas librerias <iostream> se utiliza para para incluir la biblioteca de entrada y salida estándar
y la libreria de usiganamespace se utiliza ahorrar poner std:: a la hora de imprimir o mostrar en pantalla.

se hizo las categorios aplicaciones y sistemas simulacion y juegos,graficos y patrones
